# TypeScript Type Check

Run TypeScript type checking across `app/`, `api/`, `shared/`, and `drizzle/`. Uses the max-strict `tsconfig.json` (noUncheckedIndexedAccess, exactOptionalPropertyTypes, noImplicitOverride, …).

```bash
pnpm check
```
