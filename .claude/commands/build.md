# Build for Production

Vite production build. Output goes to `dist/app/`.

```bash
pnpm build
```

For the API Lambda: production build happens inside GitHub Actions via `sam build`. Do not build and deploy locally.
